{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "baseUrl": ".",
    "rootDir": "src",
    "outDir": "dist"
  },
  "files": ["src/index.ts"],

  // Related to ts-node issue (emit skipped).
  // https://github.com/TypeStrong/ts-node/issues/693
  "ts-node": {
    "transpileOnly": true
  }
}
